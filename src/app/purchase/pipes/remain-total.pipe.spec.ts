import { RemainTotalPipe } from './remain-total.pipe';

describe('RemainTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new RemainTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
